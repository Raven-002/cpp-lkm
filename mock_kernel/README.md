# mock_kernel status

This directory is intentionally kept as a placeholder for future in-repo mock
kernel headers.

Current host-mode mocking is implemented in:

- `tests/support/mock_kernel_bridge.cpp`
- `tests/support/mock_globals.hpp`

Do not treat `mock_kernel/` as an active include source in the current build.

# linux_bridge.c is only built via Kbuild (kernel headers); host compile_commands.json
# does not include it, so clangd falls back to default flags and floods the editor.
---
If:
  PathMatch: .*linux_bridge\.c
Diagnostics:
  Suppress: "*"
